#include <cstdio>

int main(){
	int n,x,a,y,b,jia = 0,yi = 0;
	scanf("%d",&n);
	while(n--){
		scanf("%d%d%d%d",&x,&a,&y,&b);
		if(a != b && a == x + y){
			yi++;
		}
		if(a != b && b == x + y){
			jia++;
		}
	}
	printf("%d %d",jia,yi);
	return 0;
}
