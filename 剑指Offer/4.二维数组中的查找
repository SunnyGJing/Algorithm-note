"""
Creat time: 2019-06-27 19：16
Last modified time: -----

有一个二维数组，从左到右从上到下地递增排序。判断该二维数组中，是否存在某个给定的数字。

1种Solution
复杂度为O(n)+o(1)
"""

class Solution {
public:
    bool Find(int target, vector<vector<int> > array) {
        int width = array[0].size();
        int high = array.size();
        int i = 0;
        int j = width-1;
        while(i < high && j >= 0) {
            if(target > array[i][j])
                i++;
            else if(target < array[i][j])
                j--;
            else
                return true;
        }
        return false;
    }
};