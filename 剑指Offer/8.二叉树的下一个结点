"""
Creat time: 2019-07-24 10:34
Last modified time: 

已知一个二叉树结点，在中序遍历中，找出它的下一个结点。注：next指向父节点

思路：
1.当前结点没有右孩子，是左孩子则下一个结点是他的父结点；
2.当前结点没有右孩子，是右孩子则沿父结点方向寻找第一个有左孩子的结点；
3.当前结点有右孩子，则在中序遍历中，下一个结点是它的右子树的最左侧结点。
"""

struct TreeLinkNode {
    int val;
    struct TreeLinkNode *left;
    struct TreeLinkNode *right;
    struct TreeLinkNode *next;
    TreeLinkNode(int x) :val(x), left(NULL), right(NULL), next(NULL) {
        
    }
};

class Solution {
public:
    TreeLinkNode* GetNext(TreeLinkNode* pNode)
    {
        if(pNode==NULL) 
            return NULL;
        if(pNode->right!=NULL) {
            pNode = pNode->right;
            while(pNode->left != NULL)
                pNode = pNode->left;
            return pNode;
        }
        else {
            while(pNode->next!=NULL) {
                if(pNode==pNode->next->left)
                    return pNode->next;
                pNode = pNode->next;
            }
        }
        return NULL;
    }
};