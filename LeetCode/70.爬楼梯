"""
一只青蛙一次可以跳上1级台阶，也可以跳上2级。求该青蛙跳上一个n级的台阶总共有多少种跳法（先后次序不同算不同的结果）。
"""

class Solution {
public:
    int climbStairs(int n) {
        const int N = 100000;
        int ans[N];
        //ans[0] = -1;
        ans[1] = 1;
        ans[2] = 2;
        if(n > 2){
            for(int i=3; i<=n; i++){
                ans[i] = ans[i-1]+ans[i-2];
            }
        }
        return ans[n];
    }
};
